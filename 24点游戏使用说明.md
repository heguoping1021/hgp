# 24点游戏

这是一个用Java编写的24点游戏，通过控制台进行交互。

## 游戏简介

24点游戏是一个经典的数学益智游戏。玩家需要使用给定的四个数字，通过加法(+)、减法(-)、乘法(*)、除法(/)四种运算符，以及括号来改变运算顺序，使最终结果等于24。

## 项目结构

```
src/
└── com/
    └── hgp/
        └── game24/
            ├── GameMain.java           # 程序入口
            ├── Game24.java             # 游戏主逻辑
            └── ExpressionEvaluator.java # 表达式计算器
```

## 编译和运行

### 编译项目
```bash
javac -d . src/com/hgp/game24/*.java
```

### 运行游戏
```bash
java com.hgp.game24.GameMain
```

### 运行演示
```bash
javac -cp . Demo.java
java Demo
```

## 游戏规则

1. 系统随机生成4个数字（1-13之间）
2. 玩家需要使用这4个数字，每个数字必须且只能使用一次
3. 可以使用的运算符：`+`、`-`、`*`、`/`
4. 可以使用括号 `()` 来改变运算优先级
5. 计算结果必须等于24才算获胜

## 游戏功能

- **自动验证**: 系统会检查输入的表达式是否符合规则
- **提示功能**: 输入 `hint` 获得解题提示
- **跳过功能**: 输入 `skip` 跳过当前题目
- **继续游戏**: 每轮结束后可选择继续或退出

## 使用示例

### 游戏开始界面
```
===============================
      欢迎来到24点游戏！       
===============================
游戏规则：
1. 系统会给出4个数字
2. 使用 +、-、*、/ 四种运算符
3. 可以使用括号改变运算顺序
4. 每个数字必须且只能使用一次
5. 计算结果等于24即为获胜
6. 输入'hint'可获得提示
7. 输入'skip'跳过当前题目
===============================

本轮数字是：[3, 8, 1, 0]
```

### 有效的表达式示例
- `3 * 8 + 1 * 0` = 24
- `(1 + 2 + 3) * 4` = 24
- `8 / (1 - 2/3)` = 24
- `(9 - 1) * 3 * 1` = 24

### 特殊命令
- 输入 `hint`: 显示解题提示
- 输入 `skip`: 跳过当前题目
- 游戏结束时输入 `y` 或 `n` 选择是否继续

## 技术特点

1. **表达式解析**: 使用逆波兰表达式（后缀表达式）进行计算
2. **运算符优先级**: 正确处理乘除法优先于加减法
3. **括号支持**: 完整支持括号嵌套
4. **输入验证**: 严格验证用户输入的合法性
5. **错误处理**: 友好的错误提示信息

## 算法说明

### 中缀转后缀表达式
游戏使用调度场算法（Shunting Yard Algorithm）将中缀表达式转换为后缀表达式，然后使用栈来计算结果。

### 表达式验证
1. 检查字符合法性（只允许数字、运算符、括号）
2. 提取表达式中的数字
3. 验证数字是否与题目给定的数字完全匹配
4. 检查每个数字是否只使用一次

## 扩展功能

程序设计采用面向对象的方式，便于扩展：
- 可以轻松修改数字范围
- 可以添加新的运算符
- 可以实现图形界面版本
- 可以添加计时功能
- 可以添加难度级别

## 注意事项

- 输入表达式时可以包含空格，程序会自动忽略
- 除法运算会检查除数是否为零
- 浮点数计算结果使用精度比较（误差小于0.0001）
- 程序支持中文提示信息